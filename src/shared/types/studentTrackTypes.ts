export type studentTrackTypes = {
  studentId: string;
  name: string;
  phone: string;
  classTime: string;
  date: string;
  attendance: "Present" | "Absent";
};

export type PhoneFormData = {
  phone: string;
};
